
# 📘 **پایتون را قورت بده!**

✍️ *نویسنده: سیامک عباس‌نژاد*


# 🐍 **فصل ۱: شروع ماجراجویی با پایتون**

## ✨ صفحه ۱: نصب پایتون و VS Code (قدم اول برنامه‌نویسی)

سلام دوست عزیز 😄
به یکی از شیرین‌ترین و جذاب‌ترین زبان‌های برنامه‌نویسی خوش اومدی!
پایتون هم برای تازه‌کارها عالیه، هم برای حرفه‌ای‌ها.
می‌خوای وب‌سایت بسازی؟ هوش مصنوعی کار کنی؟ داده تحلیل کنی؟ یا حتی بازی بسازی؟ 🎮
پایتون کنارت هست.

در این فصل یاد می‌گیری:

* چطور پایتون رو نصب و اجرا کنی
* چطور محیط برنامه‌نویسی Visual Studio Code رو آماده کنی
* و اولین برنامه‌ات رو بنویسی ✨

---

## 🐍 مرحله ۱: نصب پایتون روی سیستم

۱. برو به سایت رسمی پایتون:
🔗 [https://www.python.org/downloads/](https://www.python.org/downloads/)

۲. آخرین نسخه (مثلاً Python 3.x.x) رو دانلود کن.
۳. موقع نصب، حتماً تیک گزینه‌ی ✅ **“Add Python to PATH”** رو بزن تا بعداً دردسر نداشته باشی.
۴. بعد از نصب، برای اطمینان باز کن **Command Prompt (CMD)** یا **ترمینال** و بنویس:

```bash
python --version
```

📤 خروجی مثلاً:

```
Python 3.12.1
```

یعنی پایتون با موفقیت نصب شده 😎🎉

---

## 💻 مرحله ۲: نصب و راه‌اندازی Visual Studio Code

حالا نیاز داریم یه محیط کار (Editor) قشنگ و حرفه‌ای برای نوشتن کدها داشته باشیم.
پیشنهاد من: **VS Code** از مایکروسافت.

۱. برو به:
🔗 [https://code.visualstudio.com/](https://code.visualstudio.com/)

۲. دانلود و نصبش کن.
۳. بعد از باز شدن، از بخش **Extensions** (آیکون آجرها در نوار سمت چپ 🧱)
جستجو کن: `Python` و نصبش کن (توسعه‌دهنده: Microsoft).

حالا VS Code آماده‌ست تا باهاش کدنویسی کنیم.

---

## ⚙️ مرحله ۳: اجرای اولین برنامه پایتونی

یه فایل جدید بساز به اسم:
📄 `hello.py`

داخلش بنویس 👇

```python
# My first Python program!
print("سلام پایتون!")  # Prints a Persian message
```

حالا دکمه‌ی ▶️ (Run) بالا رو بزن یا از منو:

```
Run > Run Without Debugging
```

📤 خروجی در ترمینال پایین VS Code:

```
سلام پایتون!
```

تبریک 🎉
تو اولین برنامه‌ت رو با موفقیت اجرا کردی!

---

## ✨ مرحله ۴: اجرای کد از ترمینال

می‌خوای مثل حرفه‌ای‌ها از خط فرمان هم اجرا کنی؟
برو داخل پوشه‌ی فایل و بنویس:

```bash
python hello.py
```

📤 خروجی:

```
سلام پایتون!
```

---

## 🧠 مرحله ۵: خط فرمان تعاملی (Interactive Mode)

اگه فقط بنویسی `python` و Enter بزنی، وارد محیط تعاملی می‌شی که می‌تونی خط‌به‌خط کد بزنی:

```python
>>> print("Hello World")
Hello World
>>> 2 + 2
4
```

برای خروج، بنویس:

```python
exit()
```

---

## 💬 نکته‌ی طلایی:

وقتی داری یاد می‌گیری، از اشتباه نترس! 😄
پایتون طوری طراحی شده که خطاهاش قابل فهم باشن. هر خطا یه سرنخه برای پیشرفتت.

---

## 💪 تمرین‌های ساده برای پایان فصل ۱

1. نام خود را بنویسید در متغیر بنویس و ان را چاپ کن
   ```python
   name = "Siyamak"
   print("سلام", name, "😊 خوش اومدی به دنیای پایتون!")
   ```

2. یک عدد را در متغیر قرار بده و در 2 ضرب کن

   ```python
   num = 22
   print("دو برابرش میشه:", num * 2)
   ```

3. جمله‌ای با فارسی چاپ کن که داخلش از چند متغیر استفاده شده باشه.

---

### 🔗 **کدها و تمرین‌های این فصل در گیت‌هاب نویسنده:**

👨‍💻 [Siamak Abbasnejad — pydevcasts](https://github.com/pydevcasts)


