# 📘 **صفحه ۱۰ — حلقه‌های تو در تو (Nested Loops) 🔁⭐**

---

## 💬 مقدمه

گاهی وقت‌ها باید یه کار رو داخل یه کار تکراری دیگه انجام بدی! 😄
مثلاً بخوای جدول ضرب بسازی، یا یه الگوی چندسطحی چاپ کنی.
اینجا جاییه که از **حلقه‌های تو در تو** استفاده می‌کنیم.

به زبان ساده یعنی:

> یه حلقه داخل یه حلقه‌ی دیگه قرار می‌گیره،
> و برای هر بار اجرای حلقه‌ی بیرونی، حلقه‌ی داخلی چند بار تکرار می‌شه.

---

## 🧩 مثال ساده ۱ — درک اجرای حلقه‌های تو در تو

بیا با یه مثال خیلی کوچیک شروع کنیم تا خوب بفهمیم چی داره می‌گذره 👇

```python
for i in range(2):
    for j in range(2):
        print(i)
```

📤 خروجی:

```
0
0
1
1
```

---

## 🔍 مرحله‌به‌مرحله دیباگ و تحلیل اجرا:

| مرحله | مقدار i | مقدار j | چاپ‌شده |
| :---: | :-----: | :-----: | :-----: |
|   1   |    0    |    0    |    0    |
|   2   |    0    |    1    |    0    |
|   3   |    1    |    0    |    1    |
|   4   |    1    |    1    |    1    |

🔹 یعنی:

* اول `i = 0` میشه → بعد حلقه‌ی `j` دو بار اجرا میشه (برای j=0 و j=1)
* بعد `i = 1` میشه → دوباره حلقه‌ی `j` دو بار اجرا میشه.

در نتیجه هر مقدار از `i` **دو بار** چاپ میشه چون حلقه‌ی داخلی برای هر `i` دوبار می‌چرخه 🔁

---

## 🧠 حالا اگه فقط `j` رو چاپ کنیم چی میشه؟

```python
for i in range(2):
    for j in range(2):
        print(j)
```

📤 خروجی:

```
0
1
0
1
```

---

## 🔍 مرحله‌به‌مرحله تحلیل:

| مرحله | مقدار i | مقدار j | چاپ‌شده |
| :---: | :-----: | :-----: | :-----: |
|   1   |    0    |    0    |    0    |
|   2   |    0    |    1    |    1    |
|   3   |    1    |    0    |    0    |
|   4   |    1    |    1    |    1    |

🔹 یعنی:

* برای هر `i`، حلقه‌ی داخلی از `j=0` تا `j=1` می‌چرخه.
* چون فقط `j` چاپ میشه، ما خروجی تکراری `0, 1, 0, 1` می‌بینیم.

---

## 🧩 مثال ۳ — چاپ هر دو مقدار با هم

حالا هر دو متغیر رو با هم چاپ کنیم تا ببینیم دقیقاً چه ترکیبی از `i` و `j` ساخته میشه 👇

```python
for i in range(2):
    for j in range(2):
        print("i =", i, ", j =", j)
```

📤 خروجی:

```
i = 0 , j = 0
i = 0 , j = 1
i = 1 , j = 0
i = 1 , j = 1
```

این یعنی:

> هر مقدار از `i` با هر مقدار از `j` ترکیب میشه.

پس تعداد کل تکرارها = تعداد `i`ها × تعداد `j`ها
(اینجا: ۲ × ۲ = ۴ بار)

---

## 🧠 نکته خیلی مهم:

حلقه‌ی داخلی **هر بار به طور کامل اجرا میشه** برای هر بار اجرای حلقه‌ی بیرونی.

به عبارت دیگه:

```
for i in range(2):   # حلقه بیرونی -> فقط دو بار می‌چرخه
    for j in range(2):  # حلقه داخلی -> برای هر i، دوبار می‌چرخه
```

🔹 یعنی جمعاً: 2 * 2 = 4 بار اجرای print() 😎

---

## 🧩 مثال ۴ — استفاده از end برای چاپ در یک خط

اگه بخوای خروجی مرتب‌تر و در قالب جدولی چاپ بشه، از پارامتر `end` استفاده کن 👇

```python
for i in range(2):
    for j in range(2):
        print(j, end=" ")
    print()  # برای رفتن به خط بعدی
```

📤 خروجی:

```
0 1
0 1
```

🔸 این شکل از حلقه‌ها پایه‌ی ساخت جدول ضرب، الگوهای ستاره‌ای ⭐ و الگوریتم‌های دوبعدی هست.

---

## ✨ تمرین‌های ساده برای کاربر:

1️⃣ برنامه‌ای بنویس که با دو حلقه‌ی تو در تو، جدول ضرب ۱ تا ۵ رو چاپ کنه.
2️⃣ الگوی زیر رو چاپ کن:

```
*
**
***
****
```

3️⃣ با دو حلقه‌ی تو در تو، همه‌ی ترکیب‌های (i, j) برای i از ۱ تا ۳ و j از ۱ تا ۴ رو چاپ کن.

---

### 🔗 **کدها و تمرین‌های این بخش در گیت‌هاب نویسنده:**

👨‍💻 [Siamak Abbasnejad — pydevcasts](https://github.com/pydevcasts)
-