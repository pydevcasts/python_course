
# 📘 **صفحه ۸ — حلقه for و تابع range() در پایتون 🔄🐍**

---

## 💬 مقدمه

خب تا حالا دیدی لازم بشه یه کار رو چند بار پشت سر هم انجام بدی؟
مثلاً بخوای ۱۰ بار یه جمله چاپ کنی، یا روی اعضای یه لیست بگردی؟
اینجاست که **حلقه‌ها (Loops)** وارد می‌شن 👣

خوب شاید با خودت بگی ما اینکار و میتوانیم با حلقه ‍‍`while` انجام بدیم!
بله کاملا درسته 

در پایتون دو نوع حلقه‌ی اصلی داریم:
1️⃣ `for`
2️⃣ `while`

حالا بریم ببینیم حلقه‌ی `for` چیه، چطوری کار می‌کنه و چه فرقی با `while` داره 💪

---

## 🌀 حلقه for چیست؟

حلقه‌ی `for` برای **تکرار روی اعضای یک مجموعه** استفاده می‌شه.
یعنی خودش دونه‌دونه از اعضای یه لیست، رشته، تاپل یا بازه عددی رد می‌شه و کاری که گفتی رو انجام می‌ده.

مثلاً:

```python
for i in [1, 2, 3]:
    print(i)
```

📤 خروجی:

```
1
2
3
```

هر بار یکی از اعضای لیست انتخاب می‌شه و داخل متغیر `i` قرار می‌گیره.

---

## 🎯 تابع range() — تولید عددهای پشت‌سرهم

تابع `range()` یکی از محبوب‌ترین چیزها تو پایتونه، چون می‌تونه **اعداد متوالی تولید کنه**، بدون اینکه حافظه زیادی مصرف کنه 💨

فرمولش اینه 👇

```python
range(start, stop, step)
```

* 🟢 `start`: عدد شروع (اختیاری — پیش‌فرض 0)
* 🔴 `stop`: تا این عدد می‌ره ولی خودشو شامل نمی‌شه
* 🔵 `step`: گام حرکت (اختیاری — پیش‌فرض 1)

---

## 📍 مثال‌های ساده با for و range()

### ✅ مثال ۱ — چاپ اعداد از ۰ تا ۴:

```python
for i in range(5):
    print(i)
```

📤 خروجی:

```
0
1
2
3
4
```

---

### ✅ مثال ۲ — چاپ اعداد از ۲ تا ۶:

```python
for i in range(2, 7):
    print(i)
```

📤 خروجی:

```
2
3
4
5
6
```

---

### ✅ مثال ۳ — پرش دو‌تایی با step:

```python
for i in range(0, 10, 2):
    print(i)
```

📤 خروجی:

```
0
2
4
6
8
```

---

### ✅ مثال ۴ — پیمایش برعکس 🔁:

```python
for i in range(10, 0, -1):
    print(i)
```

📤 خروجی:

```
10
9
8
...
1
```

---

## 💡 حلقه for فقط برای عدد نیست!

میتونی روی هر **چیزی که تکرارپذیره (iterable)** مثل رشته یا لیست هم بگردی 😍

```python
for letter in "Python":
    print(letter)
```

📤 خروجی:

```
P
y
t
h
o
n
```

و روی لیست هم:

```python
for name in ["Ali", "Sara", "Reza"]:
    print("سلام", name)
```

📤 خروجی:

```
سلام Ali
سلام Sara
سلام Reza
```

---

## 🔁 تفاوت for و while در یک نگاه 🤔

| ویژگی                  | for                  | while                   |
| :--------------------- | :------------------- | :---------------------- |
| 🔢 هدف                 | تکرار مشخص           | تکرار تا وقتی شرط درسته |
| ⏱ ساختار               | `for i in range(n):` | `while condition:`      |
| 🧮 شمارنده             | خودش داره (با range) | باید دستی بنویسی        |
| ⚠️ احتمال بی‌نهایت شدن | خیلی کم              | زیاد (اگه شرط قطع نشه)  |

---

### 🔸 مثال مقایسه‌ای:

#### 👇 با for:

```python
for i in range(5):
    print(i)
```

#### 👇 با while:

```python
i = 0
while i < 5:
    print(i)
    i += 1
```

📤 خروجی هر دو:

```
0
1
2
3
4
```

---

## ⚠️ نکته مهم:

در `while` اگه فراموش کنی شرط رو تغییر بدی، برنامه‌ت **تا ابد می‌چرخه!** 😅
ولی در `for` چون محدوده‌ی `range` مشخصه، این اتفاق نمی‌افته.

---

## 🎯 تمرین‌های کوتاه برای تمرین 💪

1️⃣ با استفاده از `for` عددهای زوج بین ۰ تا ۲۰ رو چاپ کن.

2️⃣ مجموع عددهای ۱ تا ۱۰ رو حساب کن.

3️⃣ یه رشته از کاربر بگیر و هر حرفش رو جدا چاپ کن.

4️⃣ با `while` عددی از کاربر بگیر و تا وقتی منفی نیست، ادامه بده.

---

### 🔗 **کدها و تمرین‌های این بخش در گیت‌هاب نویسنده:**

👨‍💻 [Siamak Abbasnejad — pydevcasts](https://github.com/pydevcasts)

---

