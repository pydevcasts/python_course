
# 📘 **صفحه ۱۱ — الگوهای ستاره‌ای و شکل‌های تکراری با حلقه‌های تو در تو ⭐🔁**

---

## 💬 مقدمه

الان که یاد گرفتی چطور حلقه‌های تو در تو کار می‌کنن، وقتشه باهاشون **چیزهای قشنگ بسازیم!** ✨
الگوهای متنی، مثل **مثلث، مربع یا پله‌ای از ستاره‌ها**، یکی از تمرین‌های محبوب و کلاسیک پایتون هستن.
با همین تمرین‌های ساده می‌تونی طرز تفکر الگوریتمی‌ت رو قوی‌تر کنی 💪

---

## 🧩 مثال ۱ — مربع ساده از ستاره‌ها ⭐

```python
for i in range(3):         # حلقه بیرونی: تعداد سطرها
    for j in range(3):     # حلقه داخلی: تعداد ستون‌ها
        print("*", end=" ")
    print()                # برای رفتن به خط بعد
```

📤 خروجی:

```
* * *
* * *
* * *
```

🔍 توضیح:

* حلقه‌ی بیرونی `i` کنترل می‌کنه چند سطر چاپ بشه.
* حلقه‌ی داخلی `j` کنترل می‌کنه در هر سطر چند ستاره چاپ بشه.
* دستور `end=" "` باعث میشه ستاره‌ها در یک خط چاپ بشن و با فاصله جدا بشن.

---

## 🧩 مثال ۲ — مثلث راست‌چین از ستاره‌ها ⛰️

```python
for i in range(1, 6):
    for j in range(i):
        print("*", end="")
    print()
```

📤 خروجی:

```
*
**
***
****
*****
```

🔍 توضیح:

* حلقه‌ی بیرونی مشخص می‌کنه چند سطر داشته باشیم (۵ سطر).
* در هر سطر، به تعداد شماره‌ی سطر (`i`) ستاره چاپ میشه.
* یعنی در سطر اول ۱ تا، در سطر دوم ۲ تا و الی آخر.

---

## 🧩 مثال ۳ — مثلث معکوس 🔽

```python
for i in range(5, 0, -1):
    for j in range(i):
        print("*", end="")
    print()
```

📤 خروجی:

```
*****
****
***
**
*
```

🔍 توضیح:

* از ۵ شروع می‌کنیم و هر بار یکی کم می‌کنیم.
* با این روش الگوی مثلثی به شکل برعکس درست میشه.

---

## 🧩 مثال ۴ — مثلث راست‌چین زیبا 🌙

گاهی می‌خوای مثلثت از سمت راست شروع بشه (راست‌چین).
برای این کار، باید قبل از هر سطر، چند تا **فاصله** چاپ کنی.

```python
for i in range(1, 6):
    print(" " * (5 - i) + "*" * i)
```

📤 خروجی:

```
    *
   **
  ***
 ****
*****
```

🔍 توضیح:

* `" " * (5 - i)` تعداد فاصله‌ها رو تعیین می‌کنه.
* `"*" * i` تعداد ستاره‌های اون خط رو تعیین می‌کنه.
* وقتی کنار هم چاپ می‌شن، مثلث راست‌چین تشکیل میدن 🌟

---

## 🧩 مثال ۵ — الگوی شطرنجی ساده ♟️

```python
for i in range(5):
    for j in range(5):
        if (i + j) % 2 == 0:
            print("⬛", end="")
        else:
            print("⬜", end="")
    print()
```

📤 خروجی (نمونه‌ای از یک صفحه ترمینال):

```
⬛⬜⬛⬜⬛
⬜⬛⬜⬛⬜
⬛⬜⬛⬜⬛
⬜⬛⬜⬛⬜
⬛⬜⬛⬜⬛
```

🔍 توضیح:

* اگر جمع `i + j` زوج باشه، مربع سیاه چاپ می‌کنیم.
* اگر فرد باشه، مربع سفید چاپ می‌کنیم.
* اینجوری یه الگویی شبیه صفحه شطرنج ساخته میشه 🏁

---

## 💡 تمرین برای دانشجو 🎯

1️⃣ برنامه‌ای بنویس که مثلثی از اعداد چاپ کنه:

```
1
12
123
1234
12345
```

2️⃣ برنامه‌ای بنویس که مثلث معکوس از اعداد چاپ کنه.

3️⃣ برنامه‌ای بنویس که با حلقه‌های تو در تو الگوی زیر رو چاپ کنه:

```
*****
 ***

  *
```

---

### 🔗 **کدها و تمرین‌های این بخش در گیت‌هاب نویسنده:**

👨‍💻 [Siamak Abbasnejad — pydevcasts](https://github.com/pydevcasts)
