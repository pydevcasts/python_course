

## پروژه: «مدیر Smart بودجه (Budget Buddy)»

یک برنامه‌ بساز که به کاربر کمک کنه **درآمد و هزینه‌ها رو ثبت کنه، دسته‌بندی کنه، و گزارش بگیره**—همه چیز رو هم داخل یک فایل ذخیره کنه تا با هر بار اجرا اطلاعات حفظ بشه.

---

### امکاناتی که باید پیاده‌سازی کنی

1. **دیتا تایپ‌ها**
   - استفاده از `int/float` برای مبلغ‌ها
   - استفاده از `str` برای تاریخ/توضیح/دسته
   - استفاده از `list/dict` برای ذخیره تراکنش‌ها

2. **ورودی/منو با حلقه**
   - یک حلقه‌ی اصلی `while` که تا وقتی کاربر گزینه خروج رو نزد ادامه داشته باشه
   - منو با گزینه‌هایی مثل:
     - افزودن تراکنش
     - نمایش لیست تراکنش‌ها
     - نمایش جمع هزینه‌ها/درآمدها
     - فیلتر بر اساس دسته
     - حذف تراکنش (با شماره)
     - خروج

3. **شرط‌ها**
   - چک کردن معتبر بودن ورودی:
     - اگر مبلغ منفی بود یا عدد نبود، خطا بده
     - اگر دسته نامعتبر بود، دوباره درخواست کن
   - تمایز `income` و `expense`
   - فیلتر کردن تراکنش‌ها بر اساس شرط‌ها

4. **فایل هندلینگ**
   - ذخیره تراکنش‌ها در یک فایل (مثلاً   `transactions.txt`)
   - موقع اجرای برنامه:
     - اگر فایل وجود داشت داده‌ها رو بخون
     - اگر نبود، فایل جدید بساز
   - هنگام اضافه/حذف، فایل رو آپدیت کن

5. **کامپرهنشن‌ها**
   - تولید لیست‌ها برای گزارش‌ها با list/dict/set comprehension
   - مثال‌های کاربردی:
     - محاسبه مجموع هزینه‌ها:
       - `sum(t['amount'] for t in transactions if t['type']=='expense')`
     - گرفتن فقط تراکنش‌های یک دسته:
       - `[t for t in transactions if t['category']==cat]`
     - ساختن دیکشنری خلاصه:
       - `{cat: sum(...) for cat in categories}`

---

### فرمت تراکنش‌ها (پیشنهادی)
هر تراکنش را به صورت دیکشنری ذخیره کن، مثلاً:

```python
{
  "id": 1,
  "type": "expense",   # یا "income"
  "amount": 250000,
  "category": "food",
  "description": "نان و خورشت",
  "date": "2026-05-08"
}
```

---

### خروجی گزارش‌های جذاب (حداقل 2 مورد)
- **گزارش ماهانه**: فقط تراکنش‌های ماه جاری را نمایش بده
- **سهم هر دسته از هزینه‌ها**: درصد یا حداقل مقدار کل هر دسته
- **بیشترین خرج**: بیشترین مبلغ هزینه در بین تراکنش‌ها
- **لیست آخرین 5 تراکنش**: مرتب‌سازی بر اساس id یا date

---

### معیار موفقیت (خیلی کاربردی)
برنامه باید این را انجام دهد:
- ثبت تراکنش جدید
- ذخیره در فایل
- بازیابی داده‌ها در اجرای بعدی
- نمایش گزارش‌ها با comprehension
- مدیریت خطا برای ورودی‌های اشتباه

