
# 📘 **صفحه ۹ — تمرین‌ها و الگوریتم‌های کاربردی با حلقه for 🔁**

---

## 🎯 هدف این صفحه:

در این صفحه یاد می‌گیری:

* چطور از `for` برای حل مسائل واقعی استفاده کنی
* چطور شرط `if` رو با حلقه ترکیب کنی
* چطور الگوریتم‌های کوچیک و مصاحبه‌محور رو با پایتون حل کنی 💼

---

## 🔹 بخش‌های پیشنهادی صفحه ۹:

### 1️⃣ مرور سریع: ترکیب `for` و `if`

یه توضیح کوتاه درباره اینکه چطور داخل `for` از شرط استفاده می‌کنیم:

```python
for i in range(10):
    if i % 2 == 0:
        print(i, "Even")
    else:
        print(i, "Odd")
```

📤 خروجی:

```
0 Even
1 Odd
2 Even
...
```

---

### 2️⃣ الگوریتم‌های ساده برای تمرین 🔢

#### ✅ مثال ۱: جمع اعداد ۱ تا n

```python
total = 0
for i in range(1, 11):
    total += i
print("Sum =", total)
```

📤 خروجی:

```
Sum = 55
```

---

#### ✅ مثال ۲: شمارش حروف خاص در رشته

```python
text = "python is powerful"
count = 0
for ch in text:
    if ch == "p":
        count += 1
print("Number of 'p':", count)
```

📤 خروجی:

```
Number of 'p': 1
```

---

#### ✅ مثال ۳: پیدا کردن بزرگ‌ترین عدد در لیست

```python
nums = [10, 4, 7, 21, 15]
max_num = nums[0]

for n in nums:
    if n > max_num:
        max_num = n
print("Max =", max_num)
```

📤 خروجی:

```
Max = 21
```

---

#### ✅ مثال ۴: محاسبه فاکتوریل عدد

```python
n = 5
fact = 1
for i in range(1, n + 1):
    fact *= i
print("Factorial:", fact)
```

📤 خروجی:

```
Factorial: 120
```

---

### 3️⃣ تمرین برای دانشجو ✍️

در پایان صفحه، چند تمرین کاربردی برای تقویت مهارتشون:

1. با استفاده از حلقه `for` مجموع اعداد زوج بین ۱ تا ۱۰۰ رو حساب کن.
2. برنامه‌ای بنویس که تعداد حروف صدادار (`a, e, i, o, u`) رو در رشته ورودی کاربر بشمره.
3. برنامه‌ای بنویس که مجموع اعداد منفی، صفر و مثبت رو در یک لیست جداگانه حساب کنه.
4. برنامه‌ای بنویس که جدول ضرب ۱ تا ۱۰ رو چاپ کنه (با دو حلقه تو در تو).

---

### 🔗 **کدها و تمرین‌های این بخش در گیت‌هاب نویسنده:**

👨‍💻 [Siamak Abbasnejad — pydevcasts](https://github.com/pydevcasts)

---

می‌خوای من همین ساختار پیشنهادی رو به یه **صفحه ۹ کامل و آماده برای کتابت** تبدیل کنم؟
(یعنی با لحن صمیمی، ایموجی، توضیحات روان و قالب نهایی آماده انتشار 📘)
