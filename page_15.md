# 📘 **صفحه ۱۵ — کار با فایل‌ها و مدیریت خطا (File I/O + Exception Handling) 📁⚠️**

---

## 💬 مقدمه

تا اینجا یاد گرفتی چطور با متغیرها، شرط‌ها، حلقه‌ها و توابع کار کنی.
اما یه سوال مهم: **اگه بخوایم داده‌ها رو برای همیشه ذخیره کنیم چی؟** 😯

وقتی برنامه تموم میشه، تمام متغیرها پاک می‌شن!
اینجاست که **کار با فایل‌ها** وارد بازی میشه 📁

همچنین ممکنه هنگام کار با فایل‌ها یا هر عملیات دیگه‌ای **خطا پیش بیاد** (مثلاً فایل وجود نداشته باشه).
برای مدیریت این خطاها از **Exception Handling** استفاده می‌کنیم ⚠️

در این صفحه یاد می‌گیری:

* چطور فایل باز کنی، بخونی و بنویسی 📝
* چطور با `with` فایل‌ها رو ایمن مدیریت کنی ✅
* چطور خطاها رو بگیریم و برنامه رو از کرش کردن نجات بدیم 🛡️

---

## 🧠 بخش اول: کار با فایل‌ها (File I/O)

### 🔹 باز کردن فایل با `open()`

برای کار با فایل‌ها در پایتون از تابع `open()` استفاده می‌کنیم.

```python
file = open("name.txt", "r")
```

این تابع دو پارامتر اصلی داره:

1. **نام فایل** (مثل `"demo.txt"`)
2. **حالت باز کردن** (Mode) — مشخص می‌کنه چطور می‌خوایم با فایل کار کنیم.

---

### 📌 حالت‌های مختلف باز کردن فایل

| حالت | معنی | توضیح |
|------|------|-------|
| `'r'` | Read (خواندن) | پیش‌فرض. فایل رو فقط می‌خونه. اگه فایل نباشه خطا میده. |
| `'w'` | Write (نوشتن) | فایل رو می‌سازه یا اگه باشه **پاک می‌کنه** و از اول می‌نویسه. |
| `'a'` | Append (الحاق) | اگه فایل باشه، **تهش اضافه می‌کنه**. اگه نباشه می‌سازه. |
| `'r+'` | Read + Write | هم می‌خونه هم می‌نویسه (بدون پاک کردن). |
| `'x'` | Create (ایجاد) | فایل جدید می‌سازه. اگه فایل باشه خطا میده. |

---

### 🔹 خواندن از فایل

سه روش اصلی برای خوندن فایل داریم:

#### ۱. `read()` — خواندن کل فایل

```python
f = open("demo.txt", "r")
content = f.read()
print(content)
f.close()
```

📤 خروجی (فرض کن داخل `demo.txt` نوشته شده):

```
سلام
این یک فایل تستی است.
خط سوم.
```

---

#### ۲. `readline()` — خواندن خط به خط

```python
f = open("demo.txt", "r")
line1 = f.readline()
line2 = f.readline()
print(line1)
print(line2)
f.close()
```

📤 خروجی:

```
سلام
این یک فایل تستی است.
```

---

#### ۳. `readlines()` — خواندن همه خطوط به صورت لیست

```python
f = open("demo.txt", "r")
lines = f.readlines()
print(lines)
f.close()
```

📤 خروجی:

```python
['سلام\n', 'این یک فایل تستی است.\n', 'خط سوم.']
```

---

### 🔹 نوشتن در فایل

برای نوشتن از متد `write()` استفاده می‌کنیم.

```python
f = open("output.txt", "w")
f.write("این متن داخل فایل ذخیره می‌شود.\n")
f.write("خط دوم هم همینطور!")
f.close()
```

✅ بعد از اجرا، فایل `output.txt` ساخته می‌شه و متن داخلش قرار می‌گیره.

---

### ⚠️ نکته مهم: فراموش نکن فایل رو ببندی!

اگه فایل رو با `close()` نبندی، ممکنه داده‌ها درست ذخیره نشن یا فایل قفل بمونه.

راه حل بهتر؟ استفاده از **`with`**!

---

## ✨ مدیریت خودکار فایل با `with`

دستور `with` به صورت خودکار فایل رو بعد از اتمام کار می‌بنده، حتی اگه خطایی پیش بیاد!

```python
with open("demo.txt", "r") as f:
    content = f.read()
    print(content)
# اینجا فایل خودکار بسته شده ✅
```

این روش **امن‌تر و حرفه‌ای‌تر** هست. همیشه از `with` استفاده کن! 😎

---

## 🧠 بخش دوم: مدیریت خطا (Exception Handling)

### ❓ خطا یا Exception چیه؟

وقتی کدی می‌نویسی که پایتون نمی‌تونه اجراش کنه، **خطا** می‌ده و برنامه متوقف می‌شه.

مثلاً:

```python
print(x)  # x تعریف نشده!
```

📤 خروجی:

```
NameError: name 'x' is not defined
```

برنامه کرش کرد! 😱

اما ما می‌تونیم با `try`/`except` جلوی کرش کردن رو بگیریم.

---

### 🔹 ساختار `try`/`except`

```python
try:
    # کدی که ممکنه خطا بده
    print(x)
except NameError:
    # اگه خطای NameError پیش اومد، این اجرا میشه
    print("متغیر x تعریف نشده است!")
```

📤 خروجی:

```
متغیر x تعریف نشده است!
```

✅ برنامه ادامه پیدا کرد و کرش نکرد!

---

### 🔹 بلوک `else` — وقتی خطایی نیست

می‌تونیم یه بخش `else` اضافه کنیم که **فقط اگه خطایی نباشه** اجرا بشه.

```python
try:
    num = int(input("یک عدد وارد کنید: "))
except ValueError:
    print("ورودی نامعتبر بود!")
else:
    print(f"عدد شما: {num}")
```

📤 اگر کاربر `5` وارد کنه:

```
عدد شما: 5
```

اگر کاربر `"abc"` وارد کنه:

```
ورودی نامعتبر بود!
```

---

### 🔹 بلوک `finally` — همیشه اجرا می‌شه

بخش `finally` **بدون توجه به خطا یا عدم خطا** اجرا می‌شه.

معمولاً برای بستن فایل‌ها یا آزاد کردن منابع استفاده می‌شه.

```python
try:
    f = open("demo.txt", "r")
    content = f.read()
except FileNotFoundError:
    print("فایل پیدا نشد!")
finally:
    print("عملیات تمام شد.")
    f.close()
```

📤 خروجی (اگر فایل نباشه):

```
فایل پیدا نشد!
عملیات تمام شد.
```

---

## 🎯 مثال ترکیبی: خواندن فایل CSV با مدیریت خطا

فرض کن یه فایل `students.csv` داریم که اسامی دانشجوها رو ذخیره کرده:

```
Ali Rezaei
Sara Ahmadi
Mohammad Karimi
```

می‌خوایم این فایل رو بخونیم و تعداد خطوطش رو چاپ کنیم، با مدیریت خطا:

```python
try:
    with open("students.csv", "r") as f:
        lines = f.readlines()
        print(f"تعداد دانشجویان: {len(lines)}")
        for line in lines:
            print(f"- {line.strip()}")
except FileNotFoundError:
    print("❌ فایل students.csv پیدا نشد!")
except Exception as e:
    print(f"❌ خطای ناشناخته: {e}")
else:
    print("✅ فایل با موفقیت خوانده شد.")
finally:
    print("پایان عملیات.")
```

📤 خروجی (اگر فایل وجود داشته باشه):

```
تعداد دانشجویان: 3
- Ali Rezaei
- Sara Ahmadi
- Mohammad Karimi
✅ فایل با موفقیت خوانده شد.
پایان عملیات.
```

📤 خروجی (اگر فایل وجود نداشته باشه):

```
❌ فایل students.csv پیدا نشد!
پایان عملیات.
```

---

## 💪 تمرین‌های صفحه ۱۵

### 📝 تمرین اول: شمارش خطوط فایل

**صورت سوال:**  
برنامه‌ای بنویسید که از کاربر نام یک فایل را بگیرد، آن را باز کند و تعداد خطوط آن را چاپ کند.  
اگر فایل وجود نداشت، پیام مناسب نمایش دهد.

**پاسخ تشریحی:**

```python
filename = input("نام فایل را وارد کنید: ")

try:
    with open(filename, "r") as f:
        lines = f.readlines()
        print(f"✅ تعداد خطوط فایل: {len(lines)}")
except FileNotFoundError:
    print(f"❌ فایل '{filename}' پیدا نشد!")
except Exception as e:
    print(f"❌ خطایی رخ داد: {e}")
```

🔍 توضیح:

* از `input()` نام فایل رو می‌گیریم.
* با `try` سعی می‌کنیم فایل رو باز کنیم.
* اگه فایل نبود، `FileNotFoundError` می‌گیریم.
* اگه خطای دیگه‌ای بود، با `Exception` کلی می‌گیریم.

---

### 📝 تمرین دوم: ذخیره اسامی در فایل

**صورت سوال:**  
برنامه‌ای بنویسید که یک لیست از اسامی را از کاربر دریافت کند (با کاما جدا شده) و آنها را در یک فایل به نام `names.txt` ذخیره کند (هر نام در یک خط).

**پاسخ تشریحی:**

```python

# دریافت اسامی از کاربر
names_input = input("اسامی را وارد کنید (با کاما جدا کنید): ")

# تبدیل رشته به لیست
names_list = names_input.split(",")

# نوشتن در فایل
try:
    with open("names.txt", "w") as f:
        for name in names_list:
            # حذف فاصله‌های اضافی و نوشتن هر نام در یک خط
            f.write(name.strip() + "\n")
    print("✅ اسامی با موفقیت در names.txt ذخیره شدند.")
except Exception as e:
    print(f"❌ خطایی رخ داد: {e}")

```

📤 مثال ورودی:

```
علی, سارا, محمد, زهرا
```

📤 محتوای فایل `names.txt` بعد از اجرا:

```
علی
سارا
محمد
زهرا
```

🔍 توضیح:

* `split(",")` رشته رو از کاما جدا می‌کنه و لیست می‌سازه.
* `strip()` فاصله‌های اضافی اطراف هر نام رو حذف می‌کنه.
* با حلقه `for` هر نام رو در یک خط می‌نویسیم (`\n` برای رفتن به خط بعد).

---

## 🧠 جمع‌بندی نکات کلیدی صفحه ۱۵

| مفهوم | توضیح |
|-------|-------|
| `open(file, mode)` | باز کردن فایل با حالت‌های `'r'`, `'w'`, `'a'`, `'r+'`, `'x'` |
| `read()` | خواندن کل محتوای فایل |
| `readline()` | خواندن یک خط از فایل |
| `readlines()` | خواندن همه خطوط به صورت لیست |
| `write(text)` | نوشتن متن در فایل |
| `with open(...) as f` | مدیریت خودکار بستن فایل (توصیه می‌شه!) ✅ |
| `try`/`except` | گرفتن و مدیریت خطاها |
| `else` | اجرای کد وقتی خطایی نباشه |
| `finally` | اجرای کد بدون توجه به خطا یا عدم خطا |
| `FileNotFoundError` | خطای مخصوص وقتی فایل وجود نداره |

---

## 🎉 تبریک!

الان دیگه می‌تونی:

* ✅ فایل باز کنی، بخونی و بنویسی
* ✅ با `with` فایل‌ها رو ایمن مدیریت کنی
* ✅ خطاها رو بگیری و نذاری برنامه کرش کنه
* ✅ یه برنامه واقعی بسازی که هم فایل کار می‌کنه هم خطاها رو مدیریت می‌کنه

برو سراغ تمرین‌ها و حسابی دستت رو گرم کن! 🔥

---

### 🔗 **کدها و تمرین‌های این فصل در گیت‌هاب نویسنده:**

👨‍💻 [Siamak Abbasnejad — pydevcasts](https://github.com/pydevcasts)
