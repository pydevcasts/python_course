

# 🐍 **فصل ۲: تصمیم‌گیری و تکرار در پایتون**

## ✨ صفحه ۵ — شروع با شرط‌ها (if / elif / else) 🧩

خب تا الان با ورودی، خروجی و متغیرها آشنا شدی.
اما همه‌ی برنامه‌ها یک ویژگی مشترک دارن: **تصمیم می‌گیرن!**
یعنی بر اساس شرایط مختلف، مسیر اجرای متفاوتی دارن.

برای همین این فصل رو با یادگیری قلب تصمیم‌گیری در پایتون شروع می‌کنیم: دستور **`if`** و دوستاش **`elif`** و **`else`** 💡

---

## 🧠 مفهوم تصمیم‌گیری در برنامه‌نویسی

فرض کن می‌خوای یه برنامه بنویسی که به کاربر بگه هوا گرمه یا سرده.
اگه دما بالاتر از ۳۰ بود، بگه “هوا گرمه ☀️”
اگه دما زیر ۲۰ بود، بگه “هوا سرده ❄️”
وگرنه بگه “هوا معتدله 🌤️”

پایتون برای این کار از ساختار زیر استفاده می‌کنه 👇

```python
if condition:
    # do something
elif another_condition:
    # do something else
else:
    # default action
```

یعنی:

* اگر شرط اول برقرار بود، اون قسمت اجرا میشه.
* اگر نبود، شرط دوم بررسی میشه.
* اگر هیچ شرطی برقرار نبود، قسمت else اجرا میشه.

---

## 🪄 بیایم با یه مثال خیلی ساده شروع کنیم

```python
temperature = 35

if temperature > 30:
    print("هوا گرمه ☀️")
```

📤 خروجی:

```
هوا گرمه ☀️
```

اما اگه بخوایم حالت‌های دیگه هم در نظر بگیریم:

```python
temperature = 22

if temperature > 30:
    print("هوا گرمه ☀️")
elif temperature < 20:
    print("هوا سرده ❄️")
else:
    print("هوا معتدله 🌤️")
```

📤 خروجی:

```
هوا معتدله 🌤️
```

---

## ⚖️ مقایسه و عملگرهای منطقی

در شرط‌ها از **عملگرهای مقایسه‌ای** استفاده می‌کنیم:

| عملگر | معنی             | مثال      |
| :---: | :--------------- | :-------- |
|  `==` | برابر است با     | `x == 10` |
|  `!=` | نابرابر است      | `x != 5`  |
|  `>`  | بزرگ‌تر است      | `x > 3`   |
|  `<`  | کوچک‌تر است      | `x < 3`   |
|  `>=` | بزرگ‌تر یا مساوی | `x >= 5`  |
|  `<=` | کوچک‌تر یا مساوی | `x <= 5`  |

و می‌تونیم چند شرط رو با عملگرهای **منطقی** ترکیب کنیم:

| عملگر | معنی                    | مثال                 |
| :---: | :---------------------- | :------------------- |
| `and` | هر دو شرط درست باشند    | `(x > 0 and x < 10)` |
|  `or` | یکی از شرط‌ها درست باشد | `(x < 0 or x > 100)` |
| `not` | برعکس کردن نتیجه شرط    | `not (x > 10)`       |

---

## 🎯 مثال ۲: بررسی سن کاربر

بیایم با ترکیب شرط‌ها یه مثال واقعی‌تر بزنیم 👇

```python
age = int(input("چند سالته؟ 👶👴: "))

if age < 13:
    print("تو یه کودک شیرینی 🍭")
elif age < 20:
    print("تو یه نوجوان پرانرژی‌ای ⚡")
elif age < 60:
    print("تو یه بزرگسال بالغی 💪")
else:
    print("سالخورده‌ی با تجربه‌ای هستی 👴💎")
```

📤 نمونه اجرا:

```
چند سالته؟ 👶👴: 24
تو یه بزرگسال بالغی 💪
```

---

## 🧩 مثال ۳: بررسی ورود کاربر با رمز

برنامه‌ای بنویسیم که از کاربر رمز بخواد و اگه درست بود، اجازه ورود بده 👇

```python
password = input("رمز عبور را وارد کن 🔐: ")

if password == "python123":
    print("دسترسی مجاز ✅ خوش آمدی! 🐍")
else:
    print("❌ رمز اشتباه است! دوباره تلاش کن.")
```

📤 خروجی نمونه:

```
رمز عبور را وارد کن 🔐: python123
دسترسی مجاز ✅ خوش آمدی! 🐍
```

---

## 🧠 نکته حرفه‌ای — مقداردهی بولی

تو پایتون نتیجه‌ی مقایسه‌ها **Boolean** هستن یعنی فقط `True` یا `False` می‌شن 👇

```python
x = 10
y = 5
print(x > y)  # True
print(x < y)  # False
```

حتی می‌تونی شرط رو مستقیماً در متغیر بریزی:

```python
is_adult = (age >= 18)
print(is_adult)  # True یا False
```

---

## ⚙️ تمرینات پایان فصل ۲ - بخش اول

🧩 **تمرین ۱:**
برنامه‌ای بنویس که عددی از کاربر بگیرد و تشخیص دهد که **مثبت، منفی یا صفر** است.

🧩 **تمرین ۲:**
برنامه‌ای بنویس که عددی از کاربر بگیرد و بگوید **عدد زوج است یا فرد**.

🧩 **تمرین ۳:**
برنامه‌ای بنویس که سن کاربر را بگیرد و بر اساس سنش بگوید آیا مجاز به رانندگی است یا نه (۱۸ سال به بالا ✅).

---

💬 تا اینجا یاد گرفتیم:
✅ تصمیم‌گیری با `if`, `elif`, `else`
✅ مقایسه‌ی عددی و منطقی
✅ ساختار درست‌نویسی شرط‌ها
✅ استفاده از مقایسه‌ها و عملگرهای ترکیبی

---

> «برو صفحه ۲ از فصل ۲»
